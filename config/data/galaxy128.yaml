defaults:
  - base_galaxy

name: galaxy128
dataset: galaxy128 # actually this is all galaxy dataset (we are using single dataset) but adding 128 so that can use different normalization if needed

kwargs:
  dataset_kwargs:
    resolution: 128
