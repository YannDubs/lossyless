defaults:
  - base_runtime
  
name: ???
dataset: ???

kwargs:
  data_dir: ${paths.data}
  batch_size: 128
  reload_dataloaders_every_epoch: ${trainer.reload_dataloaders_every_epoch}
  num_workers: 16
  dataset_kwargs:
    is_normalize: True