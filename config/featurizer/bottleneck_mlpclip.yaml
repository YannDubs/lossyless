# @package _global_
defaults:
  - neural_feat@featurizer
  - override /architecture@encoder: mlp_clip
  - override /distortion: ince_text
  - override /rate: H_hyper
  - override /finetune: clip_freezer

featurizer:
  name: bottleneck_mlpclip
  loss:
    beta_anneal: linear

rate:
  kwargs: 
    warmup_k_epoch: 1 # really important to warmup