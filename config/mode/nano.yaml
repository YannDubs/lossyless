# @package _global_

# check that everything is running correctly (2 epochs normal)
timeout: 60

other:
  is_quick: True

trainer:
  max_epochs: 2

logger:
  wandb:
    tags: [nano, quick]
    anonymous: true
    project: tmp