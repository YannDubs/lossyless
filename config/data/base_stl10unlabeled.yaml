defaults:
  - base

dataset: stl10unlabelled

kwargs:
  val_size: 0.05 #! very little labels so that might make an impact ? 
  dataset_kwargs:
    equivalence: [x_translation, y_translation,rotation,scale,color] #! to tune 