# @package _global_
defaults:
  - base@distortion

distortion:
  name: lossy_Z 
  factor_beta: 1 # ?
  p_norm: 1
  mode: lossy_Z # minimizes reconstruction of z
  
data_feat:
  kwargs:
    dataset_kwargs:
      additional_target:  "target" # anything woiuld work as we won't be using that

