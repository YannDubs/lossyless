# @package _global_
defaults:
  - neural_feat@featurizer
  - override /architecture@encoder: clip
  - override /distortion: ince
  - override /rate: lossless
  - override /finetune: freezer

featurizer:
  name: clip_freeze

encoder:
  z_dim: 1024

distortion:
  kwargs:
    temperature: 0.01
    is_cosine: True
    is_project: True # puts true so that some parameters exist (trick / bad practice)