defaults:
  - base

name: stl10
dataset: stl10

kwargs:
  val_size: 0.05 #! very little labels so that might make an impact ? 
  dataset_kwargs:
    equivalence: [x_translation, y_translation,rotation,scale,color] #? should we also use augmentations when training the classifier ?