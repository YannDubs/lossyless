# @package _group_
name: galaxy64
dataset: galaxy

kwargs:
  dataset_kwargs:
    root: "/checkpoint/karenu/data/"
    resolution: 64
    equivalence: [x_translation, y_translation]
    is_augment_val: True
