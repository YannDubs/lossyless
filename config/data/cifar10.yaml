defaults:
  - base

name: cifar10
dataset: cifar10

kwargs:
  # make sure you use entire training set
  val_size: 0.05
  dataset_kwargs:
    equivalence: [x_translation, y_translation,rotation,scale,color] 
    is_augment_val: True