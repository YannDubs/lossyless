# @package _global_
loss:
  name: vae
  mode: direct
  kwargs: 
      is_classification: ${data.is_classification_aux}

data:
  kwargs:
    dataset_kwargs:
      additional_target: "input"
      n_inputs: 1