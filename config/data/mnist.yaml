defaults:
  - base

name: mnist
dataset: mnist

kwargs:
  # make sure you use entire training set
  val_size: 0.05
  dataset_kwargs:
    equivalence: [x_translation, y_translation,rotation,scale] # don't use color changin because black and white
    is_augment_val: True
    is_normalize: False