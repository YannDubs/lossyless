
defaults:
  - base_galaxy

name: galaxy64
dataset: galaxy64 # actually this is all galaxy dataset (we are using single dataset) but adding 64 so that can use different normalization if needed

kwargs:
  dataset_kwargs:
    resolution: 64
